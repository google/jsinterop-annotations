bcr_test_module:
  matrix:
    platform:
      - macos
      - ubuntu2004
    bazel:
      - 8.x
  tasks:
    build_test:
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - '@jsinterop_annotations//:all'
